"""
Reserved for future non-OpenAI evaluation helpers.

The current project path uses:
- `llm.feedback_prompt` to build the prompt
- `llm.feedback_engine` to call the model
- `analysis.feedback_pipeline` to orchestrate feedback generation
"""
